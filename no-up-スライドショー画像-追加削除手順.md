# スライドショー画像・BGM 追加・削除手順（管理者向け）

このサイトには **3種類のスライドショー** と **BGMプレーヤー** があります。それぞれ「ファイルの置き場所」と「表示・再生するファイルの指定方法」が違います。
12市町村を**平等に**載せられるよう、各スライドショーとBGMごとに手順をまとめました。

> ⚠ 作業前に必ず **編集するファイルのコピー（バックアップ）** を取ってください。
> 例：`slideshow-images.json` → `slideshow-images_バックアップ_20260615.json` のように複製してから編集。
> BGMの場合は `bgm-playlist.json` → `bgm-playlist_バックアップ_20260615.json` のように複製します。

> ✅ **重要：①ヒーローと②ギャラリーは `index.html` を編集しません。**
> 画像の入れ替えは **`slideshow-images.json` という1つのファイル**だけで行います（下の①②参照）。
> ✅ **BGMもHTMLやプログラムを編集しません。**
> 曲の入れ替えは **`bgm-playlist.json` という1つのファイル**だけで行います（下の④参照）。

---

## 0. 画像を用意するときの共通ルール

- **形：横長（よこなが）** がおすすめ（目安 幅1200〜1920px）。
- **形式：** `.jpg` または `.webp`（写真は `.jpg` が無難）。
- **容量：** 1枚 300KB 程度まで（重いと表示が遅くなります）。
- **ファイル名：** 半角英数字（例 `yoron_yurigahama.jpg`）。日本語名・スペースは避ける。
- **市町村が分かる名前**にしておくと、平等配置の確認がラクです。

## 0-2. BGMを用意するときの共通ルール

- **形式：** `.mp3` または `.aac`。
- **置き場所：** `hp-amami-pr-1/hp-music/`
- **ファイル名：** 半角英数字がおすすめ。日本語名・スペースはできるだけ避ける。
- **曲名：** 画面に表示したい名前を `title` に書きます。
- **市町村バランス：** 観光画像と同じく、特定の市町村に偏りすぎないようにします。

### 12市町村チェックリスト（平等配置の確認用）

各スライドショーに、なるべく全市町村が入るようにします。

- 奄美大島：☐奄美市 ☐龍郷町 ☐大和村 ☐宇検村 ☐瀬戸内町
- 喜界島　：☐喜界町
- 徳之島　：☐徳之島町 ☐天城町 ☐伊仙町
- 沖永良部島：☐和泊町 ☐知名町
- 与論島　：☐与論町

---

## ①② は同じファイル `slideshow-images.json` で管理します

①ヒーローと②ギャラリーは、**`hp-amami-pr-1/slideshow-images.json`** という1つのファイルだけで管理します。
**`index.html` はさわりません。**

- **画像の置き場所：** `hp-amami-pr-1/hp-image/`（①②共通）
- **編集するファイル：** `hp-amami-pr-1/slideshow-images.json`（これだけ）

`slideshow-images.json` の中身（例）：

```json
{
  "_使い方": "（説明文。さわらなくてOK）",
  "hero": [
    { "file": "amami-fukei.jpg", "alt": "奄美群島の青い海と亜熱帯の自然の景色" },
    { "file": "YoronTownYurigahama.jpg", "alt": "与論島 百合ヶ浜の白い砂浜と青い海" }
  ],
  "gallery": [
    { "file": "IsenTownInudabuCapeMemorial.jpg", "alt": "伊仙町 犬田布岬の慰霊塔" },
    { "file": "manguro-bU.jpg", "alt": "奄美のマングローブ林" }
  ]
}
```

- **`"hero"`** が ①トップの大きなスライドショー、**`"gallery"`** が ②下のギャラリーです。
- 1枚分は **`{ "file": "ファイル名", "alt": "写真の説明" }`** です。
  - `file` … `hp-image` フォルダ内の **ファイル名だけ**（例 `yoron.jpg`）。`hp-image/` は付けません。
  - `alt` … 写真の説明（目の不自由な方の読み上げ用。短い説明でOK）。
- 各行（`{ ... }`）の **末尾にカンマ `,`** を付けます。ただし **そのリストの一番最後の行だけはカンマを付けない**。

---

## ① トップの「ヒーロー」スライドショー（大きな主役写真）に追加・削除

`slideshow-images.json` の **`"hero": [ ... ]`** の `[` と `]` の間を編集します。

### 追加する
1. 画像ファイルを `hp-amami-pr-1/hp-image/` に入れる。
2. `"hero"` のリストの最後の行の **末尾にカンマ `,`** を足し、次の1行を追加する（最後の行はカンマ無し）。

```json
    { "file": "ここにファイル名", "alt": "写真の説明（例：与論町 百合ヶ浜）" }
```

### 削除する
- 不要な `{ "file": ... }` の行を1行まるごと消す。
- 消した結果、**`hero` リストの最後の行にカンマが残らない**ように直す。
- ⚠ ヒーローは最低でも **1枚** は残してください（先頭の1枚が最初に大きく表示されます）。

---

## ② 下部の「ギャラリー」スライドショー（#slideshow）に追加・削除

`slideshow-images.json` の **`"gallery": [ ... ]`** の `[` と `]` の間を編集します。やり方は①と同じです。

### 追加する
1. 画像ファイルを `hp-amami-pr-1/hp-image/` に入れる。
2. `"gallery"` のリストの最後の行の **末尾にカンマ `,`** を足し、次の1行を追加する（最後の行はカンマ無し）。

```json
    { "file": "ここにファイル名", "alt": "写真の説明（例：伊仙町 闘牛）" }
```

### 削除する
- 不要な `{ "file": ... }` の行を1行まるごと消す。
- 消した結果、**`gallery` リストの最後の行にカンマが残らない**ように直す。

> 💡 ①ヒーロー(`hero`)と②ギャラリー(`gallery`)で **同じ画像（同じ `file`）を両方に入れない** でください（重複NG）。
> どちらに入れたか、上のチェックリストで管理すると便利です。

### ⚠ JSON編集の注意（とても大事）
- カンマ `,` の付け忘れ・付けすぎ、`"`（ダブルクォート）や `{ }` `[ ]` の閉じ忘れがあると、
  **スライドショーが「保険の1枚」だけになったり、表示が崩れたりします。**
- 編集後、心配なときは無料のチェックサイト（「JSON 文法チェック」で検索）に貼り付けて確認すると安全です。
- 万一JSONが壊れても、`index.html` 内の「保険の1枚」が表示されるので**真っ白にはなりません**。落ち着いてJSONを直してください。

---

## ③ 公式HP（WordPressトップ）のスライドショー

公式HP（`tokyoamamikai.com`）トップの大きなスライドショーです。**上の①②とは別物**で、置き場所も指定方法も違います。

- **画像の置き場所：** `wp-content/themes/tokyo-amamikai/slide-show/`
- **表示する画像の指定：** 同じフォルダ内の **`slide-comments.json`** に書いた画像だけが表示されます。
  （フォルダに画像を置いただけでは表示されません。必ずJSONに登録します。）

`slide-comments.json` の中身（例）：

```json
{
  "YoronTownYurigahama.jpg": "与論島 百合ヶ浜",
  "TatsugoSaigoNanshu.jpg": "龍郷町 西郷南洲",
  "AmagiTownInutoNoFuta.webp": "天城町犬の門蓋"
}
```

- 「`"ファイル名": "キャプション"`」が **1枚分** です（キャプションは画面に小さく出る説明）。
- 各行の **末尾にカンマ `,`** が必要です。ただし **一番最後の行だけはカンマを付けない**。
- 全体を `{ }`（波かっこ）で囲む。

### 追加する
1. 画像ファイルを `wp-content/themes/tokyo-amamikai/slide-show/` に入れる。
2. `slide-comments.json` を開き、最後の画像の行の後ろにカンマを足し、新しい行を追加する。

例：`犬田布岬（伊仙町）` を追加する場合

```json
  "AmagiTownInutoNoFuta.webp": "天城町犬の門蓋",
  "IsenTownInudabuCapeMemorial.jpg": "伊仙町 犬田布岬"
}
```

（↑ もともと最後だった `天城町犬の門蓋` の行末に `,` を足し、新しい行を足して、最後の行はカンマ無し）

### 削除する
- `slide-comments.json` から、その画像の行を1行まるごと消す。
- 消した結果、**最後の行にカンマが残らない**ように直す（最後の行はカンマ無し）。
- 画像ファイル自体は残しておいても表示されません（JSONに無ければ出ない）。完全に消したいときはフォルダからも削除。

### ⚠ JSON編集の注意（とても大事）
- カンマ `,` の付け忘れ・付けすぎ、`"`（ダブルクォート）の閉じ忘れがあると、**スライドショー全体が表示されなくなります**。
- 編集後、心配なときは無料のチェックサイト（「JSON 文法チェック」で検索）に貼り付けて確認すると安全です。
- 反映されないときは、③の項目「変更が反映されないとき」を参照。

---

## ④ BGMプレーヤーの曲追加・削除

「♪ BGM再生」で開くBGMプレーヤーの曲リストです。
**HTMLやプログラムは編集しません。**

- **音楽ファイルの置き場所：** `hp-amami-pr-1/hp-music/`
- **編集するファイル：** `hp-amami-pr-1/bgm-playlist.json`（これだけ）

`bgm-playlist.json` の中身（例）：

```json
{
  "_使い方": "（説明文。さわらなくてOK）",
  "tracks": [
    { "file": "shima_blues.aac", "title": "島のブルース" },
    { "file": "yoronbojo.aac", "title": "与論慕情" }
  ]
}
```

- **`"tracks"`** の中に、再生したい曲を上から順番に並べます。
- 1曲分は **`{ "file": "ファイル名", "title": "曲名" }`** です。
  - `file` … `hp-music` フォルダ内の **ファイル名だけ**（例 `shima_blues.aac`）。`hp-music/` は付けません。
  - `title` … BGMプレーヤー画面に表示する曲名。
- 各行（`{ ... }`）の **末尾にカンマ `,`** を付けます。ただし **一番最後の曲だけはカンマを付けない**。

### 追加する
1. 音楽ファイルを `hp-amami-pr-1/hp-music/` に入れる。
2. `bgm-playlist.json` の `"tracks"` の最後の行の **末尾にカンマ `,`** を足し、次の1行を追加する（最後の行はカンマ無し）。

```json
    { "file": "ここに音楽ファイル名", "title": "画面に表示する曲名" }
```

### 削除する
- 不要な `{ "file": ... }` の行を1行まるごと消す。
- 消した結果、**`tracks` の最後の行にカンマが残らない**ように直す。
- 音楽ファイル自体は残しておいても再生されません（JSONに無ければ出ない）。完全に消したいときは `hp-music` フォルダからも削除。

### 並び替える
- `{ "file": ... }` の行を、再生したい順番に並べ替えます。
- カンマの付け方だけ注意してください（最後の行はカンマ無し）。

### ⚠ JSON編集の注意（とても大事）
- カンマ `,` の付け忘れ・付けすぎ、`"`（ダブルクォート）や `{ }` `[ ]` の閉じ忘れがあると、BGMリストが正しく読めません。
- 万一JSONが壊れても、BGMプレーヤー内の「予備リスト」で再生できるようにしています。落ち着いて `bgm-playlist.json` を直してください。

---

## 変更が反映されないとき（共通）

1. ページ上で **Ctrl + F5**（強制再読み込み）を押す。古い表示がキャッシュに残っているだけのことが多いです。
2. それでも③（公式HP）が変わらない場合は、JSONがキャッシュされている可能性があります。
   担当（制作者）に「`template-amami-top.php` の `slide-comments.json?v=...` の番号（例 `v=20260526`）を新しい日付に更新してほしい」と依頼してください。

---

## まとめ（早見表）

| | ①ヒーロー | ②ギャラリー | ③公式HP | ④BGM |
|---|---|---|---|---|
| 編集ファイル | `slideshow-images.json` の `hero` | `slideshow-images.json` の `gallery` | `slide-comments.json` | `bgm-playlist.json` の `tracks` |
| ファイルフォルダ | `hp-amami-pr-1/hp-image/` | `hp-amami-pr-1/hp-image/` | `テーマ/slide-show/` | `hp-amami-pr-1/hp-music/` |
| 追加方法 | JSONに `{ "file": ..., "alt": ... }` を足す | JSONに `{ "file": ..., "alt": ... }` を足す | JSONに「"名前":"説明"」を足す | JSONに `{ "file": ..., "title": ... }` を足す |
| 表示/再生条件 | **JSONに載せた画像だけ**表示 | **JSONに載せた画像だけ**表示 | **JSONに載せた画像だけ**表示 | **JSONに載せた曲だけ**再生 |
| index.html | **編集しない** | **編集しない** | 関係なし（別システム） | **編集しない** |

①②は `slideshow-images.json` だけで管理（`index.html` は編集不要）。
④BGMは `bgm-playlist.json` だけで管理（HTMLやプログラムは編集不要）。
①と②は「同じ画像を両方に入れない」、③は「①②とは別管理（公式HP用）」という整理です。
